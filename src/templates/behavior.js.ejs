/* global once */

((Drupal, once) => {
  Drupal.behaviors.<%=  props.componentName  %> = {
    attach(context/*, settings */) {
      once('<%=  props.componentNameFilename  %>', '.<%=  props.componentNameFilename  %>', context)
          .forEach((element) => {
            // Write some awesome code here
          });
    },
    detach(/*context, settings */) {
    },
  };
})(Drupal, once);
